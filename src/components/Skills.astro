---
const skills = {
  languages: {
    title: "Languages",
    icon: "💻",
    gradient: "from-blue-500 via-purple-500 to-indigo-500",
    bgGradient: "from-blue-500/10 to-purple-500/10",
    items: ["C++ (Proficient)", "C#", "Java", "Python", "C", "Bash", "JavaScript", "TypeScript"]
  },
  cloud: {
    title: "Cloud & AI Services",
    icon: "☁️",
    gradient: "from-purple-500 via-indigo-500 to-blue-500",
    bgGradient: "from-purple-500/10 to-indigo-500/10",
    items: ["Azure OpenAI", "Azure", "AWS", "DynamoDB", "S3", "Lambda", "GitHub Codespaces"]
  },
  web: {
    title: "Web Technologies",
    icon: "🌐",
    gradient: "from-cyan-500 via-purple-500 to-blue-500",
    bgGradient: "from-cyan-500/10 to-purple-500/10",
    items: ["React.js", "JavaScript", "PHP", "HTML5", "CSS3", "Astro", "Tailwind CSS"]
  },
  ai: {
    title: "AI & Machine Learning",
    icon: "🤖",
    gradient: "from-indigo-500 via-purple-500 to-pink-500",
    bgGradient: "from-indigo-500/10 to-pink-500/10",
    items: ["NLP", "Neural Networks", "Machine Learning", "GPT-4", "ChatGPT", "PyTorch"]
  }
};
---

<section id="skills" class="relative py-24 bg-gradient-to-b from-white via-slate-50 to-blue-50/50 dark:from-gray-900 dark:via-gray-850 dark:to-gray-800 overflow-hidden transition-colors duration-300">
  <!-- Animated background with enhanced visual presence -->
  <div class="absolute inset-0 opacity-40 dark:opacity-30">
    <div class="absolute top-0 left-0 w-full h-full bg-gradient-to-br from-blue-100 via-purple-100 to-indigo-100 dark:from-blue-900/30 dark:via-purple-900/30 dark:to-indigo-900/30"></div>
    <div class="absolute top-20 -left-20 w-96 h-96 bg-blue-400/30 dark:bg-blue-500/20 rounded-full blur-3xl animate-pulse"></div>
    <div class="absolute bottom-20 -right-20 w-96 h-96 bg-cyan-400/30 dark:bg-cyan-500/20 rounded-full blur-3xl animate-pulse" style="animation-delay: 1.5s;"></div>
    <div class="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-96 h-96 bg-purple-400/30 dark:bg-purple-500/20 rounded-full blur-3xl animate-pulse" style="animation-delay: 3s;"></div>
  </div>
  
  <div class="relative max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="text-center mb-16">
      <h2 class="font-heading font-bold text-5xl md:text-6xl bg-gradient-to-r from-blue-600 via-purple-600 to-indigo-600 bg-clip-text text-transparent mb-4 animate-fade-in-up">
        Languages & Technologies!
      </h2>
      <div class="w-24 h-2 bg-gradient-to-r from-blue-500 via-purple-500 to-indigo-500 mx-auto rounded-full shadow-lg"></div>
    </div>
    
    <div class="grid grid-cols-1 md:grid-cols-2 gap-8">
      {Object.entries(skills).map(([key, category], index) => (
        <div 
          class="group relative animate-fade-in-up"
          style={`animation-delay: ${(index + 1) * 200}ms`}
        >
          <!-- Gradient glow effect on hover -->
          <div class={`absolute -inset-0.5 bg-gradient-to-r ${category.gradient} rounded-3xl opacity-0 group-hover:opacity-100 blur transition-all duration-500`}></div>
          
          <!-- Card -->
          <div class="relative bg-white/80 dark:bg-gray-900/80 backdrop-blur-sm rounded-3xl p-8 shadow-xl hover:shadow-2xl transition-all duration-500 border border-gray-100/50 dark:border-gray-700/50 group-hover:border-transparent">
            <!-- Icon and Title -->
            <div class="flex items-center gap-4 mb-6">
              <div class={`text-5xl transform group-hover:scale-110 transition-transform duration-300`}>
                {category.icon}
              </div>
              <h3 class={`font-heading font-bold text-2xl bg-gradient-to-r ${category.gradient} bg-clip-text text-transparent`}>
                {category.title}
              </h3>
            </div>
            
            <!-- Skills -->
            <div class="flex flex-wrap gap-3">
              {category.items.map((skill) => (
                <span class={`
                  relative px-5 py-2.5 
                  bg-gradient-to-r ${category.bgGradient}
                  text-gray-800 dark:text-gray-200 rounded-full text-sm font-semibold
                  border border-gray-200/50 dark:border-gray-600/50
                  hover:border-transparent
                  hover:shadow-lg
                  transform hover:scale-110 hover:-translate-y-1
                  transition-all duration-300
                  cursor-default
                  group/skill
                `}>
                  <span class={`absolute inset-0 rounded-full bg-gradient-to-r ${category.gradient} opacity-0 group-hover/skill:opacity-100 transition-opacity duration-300`}></span>
                  <span class="relative group-hover/skill:text-white transition-colors duration-300">{skill}</span>
                </span>
              ))}
            </div>
          </div>
        </div>
      ))}
    </div>

    <!-- Bottom decorative text -->
    <div class="text-center mt-16">
      <p class="text-gray-600 dark:text-gray-400 text-lg">
        Constantly learning and exploring new technologies to stay at the cutting edge
      </p>
    </div>
  </div>
  
  <!-- Gradient fade-out at bottom for smooth transition -->
  <div class="absolute bottom-0 left-0 right-0 h-32 bg-gradient-to-b from-transparent to-blue-100/50 dark:to-gray-800/80 pointer-events-none"></div>
</section>
