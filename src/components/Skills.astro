---
const skills = {
  languages: ['C++', 'Java', 'C', 'Python', 'MySQL', 'Shell Scripting'],
  cloud: ['DynamoDB', 'S3', 'Lambda', 'Elasticsearch', 'Azure'],
  web: ['HTML', 'JavaScript', 'jQuery', 'PHP', 'CSS', 'React.js'],
  tools: ['Git', 'GitHub', 'PyTorch', 'GNU Debugger', 'Lex', 'Yacc', 'Laravel']
};
---

<section id="skills" class="py-20 bg-white">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="text-center mb-12">
      <h2 class="font-heading font-bold text-4xl text-gray-900 mb-4">Languages & Technologies</h2>
      <div class="w-20 h-1 bg-primary-600 mx-auto"></div>
    </div>
    
    <div class="max-w-5xl mx-auto grid md:grid-cols-2 gap-8">
      <div class="bg-gradient-to-br from-primary-50 to-white p-8 rounded-xl shadow-lg">
        <h3 class="font-heading font-bold text-2xl text-primary-700 mb-6 flex items-center gap-2">
          <span>💻</span> Programming Languages
        </h3>
        <div class="flex flex-wrap gap-2">
          {skills.languages.map(skill => (
            <span class="px-4 py-2 bg-white text-primary-700 rounded-full text-sm font-medium shadow-sm hover:shadow-md transition-shadow">
              {skill}
            </span>
          ))}
        </div>
      </div>
      
      <div class="bg-gradient-to-br from-accent-50 to-white p-8 rounded-xl shadow-lg">
        <h3 class="font-heading font-bold text-2xl text-accent-700 mb-6 flex items-center gap-2">
          <span>☁️</span> Cloud Services
        </h3>
        <div class="flex flex-wrap gap-2">
          {skills.cloud.map(skill => (
            <span class="px-4 py-2 bg-white text-accent-700 rounded-full text-sm font-medium shadow-sm hover:shadow-md transition-shadow">
              {skill}
            </span>
          ))}
        </div>
      </div>
      
      <div class="bg-gradient-to-br from-primary-50 to-white p-8 rounded-xl shadow-lg">
        <h3 class="font-heading font-bold text-2xl text-primary-700 mb-6 flex items-center gap-2">
          <span>🌐</span> Web Technologies
        </h3>
        <div class="flex flex-wrap gap-2">
          {skills.web.map(skill => (
            <span class="px-4 py-2 bg-white text-primary-700 rounded-full text-sm font-medium shadow-sm hover:shadow-md transition-shadow">
              {skill}
            </span>
          ))}
        </div>
      </div>
      
      <div class="bg-gradient-to-br from-accent-50 to-white p-8 rounded-xl shadow-lg">
        <h3 class="font-heading font-bold text-2xl text-accent-700 mb-6 flex items-center gap-2">
          <span>🛠️</span> Tools & Frameworks
        </h3>
        <div class="flex flex-wrap gap-2">
          {skills.tools.map(skill => (
            <span class="px-4 py-2 bg-white text-accent-700 rounded-full text-sm font-medium shadow-sm hover:shadow-md transition-shadow">
              {skill}
            </span>
          ))}
        </div>
      </div>
    </div>
  </div>
</section>
